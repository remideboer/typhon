---
description: Bidirectional project memory — consult and update CERs/ADRs so the codebase evolves instead of devolving
alwaysApply: true
---

# Project memory — CERs and ADRs

Memory is **bidirectional**: look back before coding, write forward when the
change lands. Stale records are as bad as ignored ones.

| Kind | Path | Level |
| --- | --- | --- |
| **CER** (code evolution) | `docs/evolution/` | Symbol / pre-post behavior |
| **ADR** (architecture) | `docs/adr/` | System boundaries and contracts |

Indexes: `docs/evolution/README.md`, `docs/adr/README.md`.

## Loop (every non-trivial change)

### 1. Look back

- Skim indexes for records touching the area (security, deps/locks, IDE,
  parse/import/pipeline, publish, perf).
- Read matching CERs/ADRs; treat **post-behavior** / **Decision** as the baseline.
- Do not reintroduce documented pre-behavior unless the user asks to reverse it.

### 2. Change the code

- Prefer extending post-behavior over “simplifying back” to pre-behavior.
- Perf: measure first (ADR-003). Security: fail closed at boundaries (ADR-001).

### 3. Write forward (same change set — not a later chore)

Update or add records **before calling the work done**:

| What happened | What to write |
| --- | --- |
| New lasting code-level fix / behavior shift | New CER entry or new CER; refresh index |
| New or changed system boundary / contract | New ADR or amend Accepted ADR; refresh index |
| Reverses a prior decision | Mark old record `Superseded by …`, link new one |
| Extends an existing decision without reversing it | Amend the living CER/ADR (post-behavior, consequences, commits) |
| Pure typo / comment / docs unrelated to decisions | No CER/ADR required |

Ship code + memory together. A PR/commit that changes a decided behavior without
touching the matching CER/ADR is incomplete.

## Anti-devolution examples (unless a new record supersedes them)

- IDE helpers: `python -I`, bundled path, no workspace `PYTHONPATH`;
  Go to Definition into locked `typhon.deps` only via opt-in
  `typhon.navigateLibrarySources` + `--library-sources` (trusted workspace);
  diagnostics stay fail-closed
- Analysis/transpile: no pip-install / no third-party import for typing by default
- `TYPHON_WORKSPACE_ROOT` → realpath containment; no `interpreter.path` in `typhon.deps`.
  When the env is unset, deps discovery also stops at the nearest `typhon.toml`
- Hashed fail-closed locks; no second `tokenize` in `compile_typhon`
- `run_source` tests for dep-free examples under this repo: set
  `TYPHON_WORKSPACE_ROOT` to the example’s directory (CER-001 §4), or give the
  example its own `typhon.toml` — never inherit the root MySQL `typhon.lock` (breaks
  Linux CI with win-amd64 platform mismatch)
- Keep parse memoization; do not re-`resolve()` already-resolved package parents without measurement + CER
- Do not force PEG (`_BRACE_ENGINE = "peg"`) without a CER showing packrat ≤ RD on the gate corpus
- Keep `parse_program_from_tokens` / single-lex compile path; do not restore double-counted phase benches
- Structs stay identity-free value types (ADR-005 / CER-004): no methods/inherits/`new`; fields always public (visibility on the struct); copy on assign/call/return; hash only when all fields fix / `fix struct`
- `data` / `entity` stay separate from `struct` (ADR-011 / CER-011): `data` = frozen VO (all-fields equality); `entity` = identity keys only (`identity(...)` fix); no `uses` on either; do not fold entity identity into `struct`
- Lambdas (ADR-012 / CER-012): capture by value at creation; read-only unless `shared` or `atomic`; foreach loop vars stay immutable per iteration; do not restore Python late-binding
- Brace scopes (CER-015): `{ }` bodies nest `declared`; loop binders and brace-local decls do not leak; emit mangles to `_typhon_bN_*` + `debug_names`; lambda capture defaults use the mangled outer name
- Atomic (ADR-013 / CER-013): `atomic <primitive> name = expr` implies shared for capture; indivisible `+=`/`-=`/`++`/`--` / `get` / `compareAndSet`; reject `*=`/`/=`/`%=` and redundant `shared`; keep `_TyphonShared` unchanged; do not add memory-order knobs or float atomics without a new record
- Debug stepping (ADR-014 / CER-014): launch generated program under debugpy (not `transpiler run` subprocess); emit `.typhonmap.json` line maps + capture `names`; remap breakpoints (in+out), stack, Variables; halt at breakpoints only (`stopOnEntry: false`); Clear All Breakpoints UX; logpoints (`logMessage` + `{expr}` rewrite); `prepare_debug` PYTHONPATH = temp + `typhon.deps` sites (parity with Run); inline values on `.typhon` while paused; `remapVariables` applies `names` before `hidePrefixes` so `_typhon_bN_*` brace locals show; do not weaken ADR-001; no scalar unwrap of shared/atomic; tasks/threads still deferred
- Find Usages (CER-016 / CER-018): ReferenceProvider + binding-aware refs (import graph + scopes); `--usages` with optional `--line`/`--column`; package-folder lexical scan is **not** the DoD path
- IDE refactoring (ADR-016 / CER-018): educational core Rename / Extract / Inline / Safe Delete / Introduce Parameter via `RefactorPlan` + preview + WorkspaceEdit; trust stays ADR-001; full Fowler catalog is F-005 only
- Member / import kind order (ADR-015 / CER-017): parse-phase section cursor; imports-only file prefix; class `const`→`fix`→field→ctor→method; struct fix→mutable; trait requires→methods; entity identity→fix→field→ctor→method; visibility unordered within a section; **not** inside `tasks { }`; educational FatalParseError (§5 messages) — do not reintroduce free-form bodies or a sem-only duplicate pass
- Enums stay nominal closed sets (ADR-006 / CER-005): all-or-nothing values; no `@alias` / match until a new record; casing is Warning + IDE quick fix, not a hard error
- Abstract classes (ADR-010 / CER-009 / ADR-028): `closed` XOR `abstract`;
  abstract methods only in abstract classes (no body; implicitly open sockets);
  nominal types; emit `ABC`/`@abstractmethod` (Python backend — users write
  `abstract`, not `@abstractmethod`); reject direct instantiate; concrete
  subclasses must `override` inherited abstracts
- Constructors (ADR-027 / CER-045): `public constructor(...)` on class and
  entity — not type-name form; omitted access defaults to `module` (same-file);
  `this(...)` → `self.__init__(...)`; implicit
  `super().__init__()` when neither `super` nor `this` chaining; bare field
  ids require `this.`; uninitialized `fix` fields assigned on every ctor path
- Extension points (ADR-028 / CER-046): methods closed by default; `open` /
  `override` / `override closed`; class-level `closed` (not `sealed`);
  implicit root `toString`/`equals`/`hashCode` for override attachment only
- Static members (ADR-029 / CER-047): `static` on class fields/methods; no
  `this` in static methods; incompatible with `open`/`override`; `static const`
  allowed but redundant; emit `@staticmethod`
- Interface methods (CER-010): no access modifier on signatures (always
  public/abstract); reject `public`/`private`/… inside `interface` bodies;
  implementing class methods still need `public`
- Library decorators (ADR-026 / CER-043 / CER-044): `@expr` may apply to
  `function` / `class` / methods for **library** callables (e.g. FastAPI). Do
  **not** use `@` to invent missing Typhon constructs (`@alias`, Lombok-style
  `@Data`) — prefer a real keyword or form. Emit-only `@abstractmethod` stays
  an implementation detail. Emit keeps PascalCase param annotations
  (`Request`) for framework DI; FastAPI field research lives under
  `library-tests/fastapi-shop/` (not `examples/`).
- Int literals / bitwise (ADR-007 / CER-006): `0b`/`0x`/`_`; width aliases are
  unsigned ranges; `and`/`or`/`not` stay logical; rotate deferred
  ([F-001](../../docs/TODO-FUTURE.md#f-001-bitwise-rotate) in `docs/TODO-FUTURE.md`)
