# statically type language leaning toward the C-family syntax, toggle to dynamic
# explicit end statement operator
;


# the hash # is used for single line comments
## dubble hash for multiline comments end with /#

# compound statement delimiters
{}

# no significant white space!!!! Code blocks are marked with the compound statement delimiters, which are mandatory to explicitly show the boundaries of the of if, while, switch, for, etc blocks

# this is a single line comment
## this is a
multi
line comment
/#

# define variable, statically typed: float, int, char, string
int x = 15;
float y = 1.23;
char ch = 'a';
string str = "Hello, World!";

# array definition, no type mixing in arrays
# the array should be the memory efficient version, so not a object
# https://realpython.com/ref/stdlib/array/
# unsized (length from initializer) and sized forms:
int[] numbers = [1, 2, 3, 4, 5]
float[] floats = [1.1, 2.2, 3.3]
bool[] flags = [true, false, true]
# fixed capacity: length must match
int[4] primes = [2, 3, 5, 8]
int[4] bad = [2, 3, 5, 8, 9] # error: array index out of bounds
# note: string[] cannot use array.array (strings are objects) and becomes a Python list
string[] names = ["John", "Jane"]

# CONTROL FLOW
# iteration, with general for loop, explicit language rule; loop counter is an invariable, so it may NOT be altered during iteration by code in de loop itself. Keyword loop
# for loop, keyword loop
loop(int i=0;i<10;i++){ print(i); print(i)} ## the compound stamenent operators are mandatory! to delinieate what is part of the loop. 
# while loop, keyword also loop
loop(x<10){ print(i); i++} # automatically recogignize a while loop


# if block
if(x<=10){ print(x); }
else if (x > 10 && x < 20) { print("somewhere in between 10 and 20"); }
else {print("something else");}

# not and unless the human legible if(!x<=10)
if(not x<=10)
unless(x>10){ print("something smaller than 10")} 

# string interpolation
string str2 = "{str} is een string, {y} is een float";

# typed string interpolation
string str_typed = "#s{} is een string, #f{y} is een float, #i{x} is een integer, #c{ch} is een character";

escape character \ standard escaped, but \# should be added as escape for the typed # interpolation operator

# explicit casting
int z = (int) y;

# top level import statements (file = module)
# import funcs                 # same as import all from funcs.typhon
# import all from funcs.typhon    # bring visible exports into scope
# import greet from funcs.typhon  # single export
import

# top-level visibility (optional; default is module-private)
# global   - whole project (global access)
# package  - same folder only
# module   - this file only (default; keyword not required)

# compile-time constants (optional visibility)
# global const float PI = 3.14159265358979323846
# const int MAX = 100

# function definition with no return type 
function my_function():
  print("Hello from a function");

# function definition with return type
function int my_returning_function():
  return 1;

# class definitions
# a Cclass inherites from Object implictly
# method overloading based on signature
# real private class members, no name mangling
# access modifiers private, protected (so acces to own class and subclass only), module (acces only in the file, like pyhton does) public
# file example.typhon
class Car{
	# attribute declaration
	private string brand;
	# constructor, same name as class, not return type, no self is used
	public Car(string brand){
		# self reference own instance
		this.brand = brand;
	}
	
	public drive(){
		print("Moving!");
	}
	
	module string brand(){
		return this.brand;
	}
}

# inheritance
class Truck inherits Car{
	# attribute declaration
	# constructor, same name as class, not return type, no self is used
	public Truck(string brand){
		super(brand);
	}
	
	public drive(){
		super.drive()
		print("\n With 18 wheels!");
	}

}

# use
Car c1 = Car("Toyota");
Truck t1 = Truck("DAF");

c1.drive();
t1.drive(); 
print("brand of the truck is {t1.brand()");

----- file other.typhon
import Car from example.typhon

Car c1 = Car("Toyota");
c1.drive();

print("brand of the car is {c1.brand()"); # error, access to c1.brand() is restricted to it's module


# dependency management like gradle / maven
# not using venv — runner refers to central repo (~/.typhon/repository) like maven .m2
# flyweight: each package/version installed once, shared across projects
# file: typhon.deps (read by transpiler/runner; pip downloads into the central repo)
[interpreter]
	version: [default any| >=3.9 | < 3.5]
	path: [for specific path]
[dependencies]
	mysql-connector-python
		version: [default latest when no version is given]
		build: [run|test] # optional
	matplotlib
	my-lib
	my-other-lib






 