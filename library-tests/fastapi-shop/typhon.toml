[project]
main = "src/main.typhon"

[source_roots]
main = "src"
test = "tests"

[interpreter]
version = ">=3.10"

[dependencies]
fastapi = { version = "0.115.6", build = "run" }
uvicorn = { version = "0.32.1", build = "run" }
"mysql-connector-python" = { version = "8.0.33", build = "run" }
bcrypt = { version = "4.2.1", build = "run" }
httpx = { version = "0.28.1", build = "run" }
