# fleet.typhon — completion
# TODO: declare package interface Drivable with start() and move() (no access modifiers)

# TODO: package class Bike implements Drivable (private string name, constructor, start/move)

# TODO: package class Truck implements Drivable (different move message)
