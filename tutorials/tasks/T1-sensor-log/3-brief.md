# T1 level C — conventional brief

## Situation

Your lab adds probe **probe-C**. After warm-up it reads **19.75°C** and is **stable**.
This is sample **3**. The probe id never changes during the run.

## Deliverable

A single `.typhon` file that prints one clear log line.

## Constraints

- Use at least one `const` or `fix` with a reason you can defend.  
- Use typed interpolation for the numeric and boolean parts.  
- No classes, no imports.

## Done when

T1 success criteria in [README.md](README.md) hold, and a peer can understand the printout without seeing your source.
