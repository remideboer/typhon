package interface Startable {
    start()
}

package class Car implements Startable {
    private string name

    public constructor(string name) {
        this.name = name
    }

    public start() {
        print("{this.name} start")
    }
}

Car c = Car("Dacia")
c.start()
Startable s = c
s.start()
