# Iterator — sequential access without exposing representation.
# Language `loop (T x in xs)` is sugar for the same idea on built-in collections.

interface IntIterator {
    bool hasNext()
    int next()
}

class ArrayIntIterator implements IntIterator {
    private list<int> data
    private int index

    public constructor(list<int> data) {
        this.data = data
        this.index = 0
    }

    public bool hasNext() {
        return this.index < len(this.data)
    }

    public int next() {
        int value = this.data[this.index]
        this.index = this.index + 1
        return value
    }
}

class IntCollection {
    private list<int> items

    public constructor() {
        this.items = []
    }

    public void add(int value) {
        this.items.append(value)
    }

    public IntIterator iterator() {
        return ArrayIntIterator(this.items)
    }
}

IntCollection nums = IntCollection()
nums.add(10)
nums.add(20)
nums.add(30)
IntIterator it = nums.iterator()
string out = ""
loop (it.hasNext()) {
    if (out != "") {
        out = out + ","
    }
    out = out + str(it.next())
}
print(out)  # 10,20,30
