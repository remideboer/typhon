# main.typhon — Pokemon TCG Tkinter demo entry point
# Run: python -m transpiler run examples/gui/pokemontcg/main.typhon

import store
import ui

AppStore appStore = openStore()
PokemonApp app = PokemonApp(appStore)
app.run()
