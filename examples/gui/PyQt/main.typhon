# main.typhon — Pokemon TCG PyQt6 demo (isolated silo)
# Run: python -m transpiler run examples/gui/PyQt/main.typhon
# QApplication must exist before constructing a QMainWindow subclass.

import QApplication from PyQt6.QtWidgets
import sys
import store
import ui

QApplication qtApp = QApplication(sys.argv)
AppStore appStore = openStore()
PokemonQtApp window = PokemonQtApp(appStore)
window.run()
qtApp.exec()
