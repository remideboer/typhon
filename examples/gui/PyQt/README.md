# Pokemon TCG (PyQt6) example — isolated silo

Master–detail UI with PyQt6 from `typhon.toml` `[dependencies]`. Click a catalog/collection/deck
row to see details instantly. Tk twin: `examples/gui/pokemontcg/`.

```bash
python -m transpiler run examples/gui/PyQt/main.typhon

# Minimal QMainWindow subclass (library inherits):
python -m transpiler run examples/gui/PyQt/hello_mainwindow.typhon
```

See **[docs/README.md](docs/README.md)**.
